//all example files are made to be uploaded to GitHub as an example for the file
//comments are added to show what data is required or missing
//this is done for security reasons and to protect database passwords, usernames, and secrets

module.exports = {
	database: //path to database
	secret: //insert a secret here
}